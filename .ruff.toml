line-length = 110
target-version = "py39"

[lint]
select = ["E", "F", "W", "UP", "B"]
ignore = [
    # в GUI-коде осознанно много «мягких» except: падение анимации или
    # обращения к реестру не должно ронять всё окно
    "B008",
]
